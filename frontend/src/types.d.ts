export type TextType = {
    text: string
}

export type UserBookType = {
    title: string,
    author: string, 
    isbn: string,
    description: string,
    image: string,
    rating: number, 
}

export type BookViewType = {
    title: string,
    author: string, 
    isbn: string,
    description: string,
    image: string,
    rating: number, 
    distance: number
}